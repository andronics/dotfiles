#!/usr/bin/env sh
gomplate --config "${dotfiles_source_root}/.gomplate/plugins.yml" \
    -f "${dotfiles_target_root}/.config/weather/config.json.tmpl" \
    -o "${dotfiles_target_root}/.config/weather/config.json" 