# bspc-manager rules configuration
# Format: <class> <options>
# Options are bspc rule options (desktop=X, state=floating, etc.)

# Development tools
firefox desktop=web
code desktop=dev state=tiled
vim desktop=dev
emacs desktop=dev

# Communication
discord desktop=chat state=floating
slack desktop=chat
thunderbird desktop=mail

# Media
vlc desktop=media state=floating
spotify desktop=media

# System tools
htop desktop=system state=floating
ranger desktop=systemcode desktop=dev
firefox desktop=web
discord desktop=chat
spotify desktop=media
terminal monitor=focused desktop=dev
code desktop=dev
firefox desktop=web
discord desktop=chat
spotify desktop=media
terminal monitor=focused desktop=dev
